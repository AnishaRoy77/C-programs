//find the sum and multiplication using if statement
#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b,c;
	printf("enter value of a & b");
	n=scanf("%d%d",&a,&b);
	if(n==2)
	{
		printf("the sum of a&b %d",a+b);
		printf("the product of a&b %d", a*b);		
		}
		getch();
	}
	
